package cjmb.com.raasrelease01;

/*Unimplemented class. This is used for taking list or map data types and converting them to
* Android views (scrolling lists, dropdown menus, etc.*/

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class SimpleArrayListAdapterActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_simple_array_list_adapter);
    }
}
